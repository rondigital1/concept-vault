# Database
# Local Docker:
# DATABASE_URL=postgres://knowledge:knowledge@localhost:5432/concept_vault
#
# Supabase (recommended for cloud deployment):
DATABASE_URL=postgresql://postgres.<project-ref>:<db-password>@aws-0-<region>.pooler.supabase.com:5432/postgres?sslmode=require&pgbouncer=true

# LLM Provider (openai or anthropic)
MODEL_PROVIDER=openai
MODEL_NAME=gpt-4o
OPENAI_API_KEY=sk-your-key-here
ANTHROPIC_API_KEY=sk-ant-your-key-here

# App Config
APP_PRIVACY_MODE_DEFAULT=standard

# Auth.js (Google OAuth, single-owner access)
AUTH_SECRET=replace-with-long-random-value
AUTH_GOOGLE_ID=google-client-id
AUTH_GOOGLE_SECRET=google-client-secret
OWNER_EMAIL=you@example.com

# Cron Auth
# Vercel cron sends Authorization: Bearer <CRON_SECRET>.
CRON_SECRET=change-me-to-a-long-random-token

# Optional route-specific cron secret overrides
# WEB_SCOUT_CRON_SECRET=optional-override
# TOPIC_REPORT_CRON_SECRET=optional-override

# Cron Goals
WEB_SCOUT_DAILY_GOAL=Find interesting high-quality articles from trusted sources that complement my vault.
